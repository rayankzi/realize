{
  "name": "realize",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test:summarize-only": "bun test --timeout 300000 tests/summarize-only.test.ts",
    "test:single-workflow": "bun test --timeout 300000 tests/single-workflow.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@lmstudio/sdk": "^1.5.0",
    "zod": "3.25.76"
  }
}
